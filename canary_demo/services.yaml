apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-canary-v2
spec:
  ports:
  - port: 80
    targetPort: 8088
    protocol: TCP
  selector:
    app: canary-demo

---
apiVersion: v1
kind: Service
metadata:
  name: rollouts-demo-stable-v1
spec:
  ports:
  - port: 80
    targetPort: 8088
    protocol: TCP
  selector:
    app: canary-demo
